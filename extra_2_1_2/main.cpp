#include <iostream>
#include <string>

using namespace std;

struct Node {
    int data;
    Node* next;

    //constructor
    Node(int data) : data(data), next(nullptr) {}
};

void merge() {
    return
}

void insert() {

}


void mergeSort(int size, int left, int right, int arr[]) {
    //base case
    if (size == 0) {
        return;
    }

    //recursive case
    if (left < right) {
        mergeSort()
    }

}

//practice
void lengthOfLLRecursive(Node*& head) {
    Node* curr = head;
    while (curr != nullptr) {
        
    }
}


int main() {
    Node l1(1);
    Node l2(2);
    Node l3 = NULL;


    //merge Nodes and return in l3, assume l1 and l2 are sorted

}